<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('../partials/headPartial.ejs') %>
  <link rel="stylesheet" href="/css/product.css">
  <title>MEMORIES</title>
</head>

<body>
<%- include('../partials/headerPartial.ejs') %>
  <div class="container-fluid">
    <div class="col-12" id="experienciasDentroDelMemorie">
      <h1 class="tituloCategoriaGrilla"> Todas las Memories </h1>
        <h4 class="subtituloCategoriaGrilla"> Disfrutá de experiencias <br> inolvidables </h4>
    </div>
    <div class="container text-center">
      <div class="btn-group">
        <button type="button" class="btn btn-secondary btn-sm dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Ordenar
        </button>
        <div class="dropdown-menu text-center">
          <a class="dropdown-item" href="/products/ver-todo/az">A / Z</a>
          <a class="dropdown-item" href="/products/ver-todo/za">Z / A</a>
          <a class="dropdown-item" href="/products/ver-todo/lowest">PRECIO MÁS BAJO</a>
          <a class="dropdown-item" href="/products/ver-todo/highest">PRECIO MÁS ALTO</a>
        </div>
      </div>
    </div>
    <div class="row botonesCategoria">
    <div class="container">
      <div class="row" id="rowMemoriesPacks">        
        <% for( let i = 0; i < products.length; i++ ) { %>
        <% if (products[i].visibility == 0 && products[i].qty >= 1) { %>
        <div class="col-xl-4 col-lg-4 col-md-4 col-sm-12" class="memoriePack"><a href="/detalle/<%= products[i].sku %>">
          <img class="imagenMemoriePack" src="/img/packs/<%= products[i].image %>" alt="<%= products[i].name %>"></a>
          <% if (products[i].price == products[i].specialPrice) { %>
          <h4 class="text-center mb-4"><%= '$' +  toThousand(products[i].price) %></h4>
          <% } else {%>
          <h4 class="text-center mb-4"><%= '$' +  toThousand(products[i].specialPrice) %></h4>
          <h4 class="text-center mb-4 specialPrice"><%= '$' +  toThousand(products[i].price) %></h4>
          <% } %> 
        </div>
        <% } %>
        <% } %>
      </div>
    </div>
  </div>
<%- include('../partials/footerPartial.ejs') %>
<%- include('../partials/jsPartials.ejs') %>
</body>

</html>